services:
  app:
    image: ${DOCKER_IMAGE:-ikapo/course-matcher-api:latest}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: course-matcher-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PORT=${PORT:-8000}
    networks:
      - app-network

  caddy:
    image: caddy:2.7-alpine
    container_name: course-matcher-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - PORT=${PORT:-8000}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      app:
        condition: service_healthy
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
